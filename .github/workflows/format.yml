name: Format

on: [push, pull_request]

jobs:
  clang-format:
    name: clang-format
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Run clang-format
      run: |
        find frontend lib -iname '*.h' -o -iname '*.c' | xargs \
        pipx run "clang-format>=17,<18" \
          --style=file \
          -i
        git diff --color --exit-code
